[project]
name = "email-triage-env"
version = "1.0.0"
description = "An OpenEnv-compliant environment for AI agent email triage tasks"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends.legacy:build"
